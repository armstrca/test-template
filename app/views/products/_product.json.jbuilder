json.extract! product, :id, : category, : inventory_transaction, : location, : order_item, : order, : report, : role, : supplier, : user, :created_at, :updated_at
json.url product_url(product, format: :json)
